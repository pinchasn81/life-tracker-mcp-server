[project]
name = "mcp-server"
version = "0.1.0"
description = "MCP server for LifeTracker - DynamoDB integration"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "fastmcp>=2.13.1",
    "boto3>=1.40.0",
    "python-dotenv>=1.0.0",
]
